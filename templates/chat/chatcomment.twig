<div class='chat_comment' style="margin-bottom:8px" data-id="{{ comment.id }}" data-date="{{ comment.datum_raw }}">
	<span class="kicsi"><b><span class="response_open" data-to="{{ comment.user }}">{{ comment.user }}</span></b> ({{ comment.datum }})<br>
	{{ comment.jelzes|raw }}<font color={{ comment.color }}>{{ comment.szoveg|raw }}</font></span>
</div>
