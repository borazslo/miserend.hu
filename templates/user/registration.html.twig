{% extends 'layout_3col.html.twig' %}
{% set title = 'Regisztráció' %}

{% block content %}
	{{ form_start(form) }}
	{{ form_widget(form) }}

	<div class="mb-3">
		<button class="btn btn-primary" type="submit">Regisztráció</button>
	</div>

	{{ form_rest(form) }}
{% endblock %}
