<!DOCTYPE html>

{% set ICONS_CHURCH_EDIT = 'fa fa-edit' %}
{% set ICONS_CHURCH_SEE = 'fa fa-eye' %}
{% set ICONS_CHURCH_PHOTOS = 'fa fa-photo-video' %}
{% set ICONS_MASSSCHEDULE = 'far fa-clock' %}
{% set ICONS_MAP_EDIT = 'fa fa-map' %}
{% set ICONS_MAP_SEE = 'fa fa-map-marker' %}
{% set ICONS_MAP_DIRECTION = 'fa fa-map-signs' %}
{% set ICONS_DELETE = 'fa fa-trash-alt' %}
{% set ICONS_DENY_ACCESS = 'fa fa-minus-circle' %}
{% set ICONS_GRANT_ACCESS = 'fa fa-plus-circle' %}
{% set ICONS_ADD = 'fas fa-plus-square' %}
{% set ICONS_INFO = 'fas fa-question-circle' %}
{% set ICONS_WHEELCHAIR = 'fa fa-wheelchair' %}
{% set ICONS_REMARKS_NO = 'far fa-envelope' %}
{% set ICONS_REMARKS_ALLDONE = 'far fa-envelope-open' %}
{% set ICONS_REMARKS_PROCESSING = 'fa fa-envelope-open-text' %}
{% set ICONS_REMARKS_NEW = 'fa fa-envelope' %}


<html lang="hu">
<head>
    {% if environment != 'production' %}
        <meta name="robots" content="noindex,nofollow">{% endif %}
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="HandheldFriendly" content="True">
    <meta name="MobileOptimized" content="320">
    <title>
        {%- block page_title -%}
            {{ title ~' | Miserend' -}}
        {%- endblock -%}
    </title>

    <meta name="description" content="{{ teaser|default('Magyarország és a Kárpát-medence katolikus templomainak miserendjei.') }}">
    <meta name="keywords" content="katolikus, római katolikus, görögkatolikus, szentmise, miserend, liturgia, ünnep">
    <meta name="author" content="miserend.hu">
    <meta http-equiv="cleartype" content="on">

    <meta name="apple-itunes-app" content="app-id=967827488">
    <meta name="google-play-app" content="app-id=com.frama.miserend.hu">

    {% block extra_meta_tags %}
        {# todo refactor #}
        {{ extraMeta|default('')|raw }}
    {% endblock %}
    <link rel="shortcut icon" type="image/vnd.microsoft.icon" href="/favicon.ico">

    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}

    {% block extra_head %}
    {% endblock %}
</head>
<body>
{% block body %}
    <script>
        (function (i, s, o, g, r, a, m) {
            i['GoogleAnalyticsObject'] = r;
            i[r] = i[r] || function () {
                (i[r].q = i[r].q || []).push(arguments)
            }, i[r].l = 1 * new Date();
            a = s.createElement(o),
                m = s.getElementsByTagName(o)[0];
            a.async = 1;
            a.src = g;
            m.parentNode.insertBefore(a, m)
        })(window, document, 'script', '//www.google-analytics.com/analytics.js', 'ga');

        ga('_setCustomVar', 1, 'design', 'new', 2);
        ga('create', 'UA-3987621-4', 'auto');
        ga('send', 'pageview');
    </script>
    <div class="container">
        <div class="row">
            <div class="col-md-3 nopadding logo"><a href="{{ path('home') }}"><img src="{{ asset('static/images/logo.png') }}" alt="Miserend oldal" loading="lazy"/></a></div>
            <div class="col-md-9 d-flex justify-content-end nopadding">
                <a href="https://kozossegek.hu" target="_blank" class="tarsoldal tkozossegek text-decoration-none">kozossegek.hu</a>
                <a href="https://vp2.hu" target="_blank" class="tarsoldal tvp2 text-decoration-none">vp2.hu</a>
            </div>
        </div>
    </div>
    <div class="menusav">
        <div class="container">
            {% include 'navbar.html.twig' %}
        </div>
    </div>
    <div class="container">
        <div class="row church-site-sidebar-holder">
            <div class="col-md-3  col-sm-3 church-site-left-sidebar">
                {% block left_sidebar %}
                    {% if chat|default(false) %}{% include 'chat/chatbox.twig' with chat %}{% endif %}
                    {% if mychurches|default(false) %}{% include "panel.twig" with {'title':'Módosítás','body': mychurches|raw } %}{% endif %}

                    {% if show_self_advertistment_panel|default(true) %}
                        {% include 'panel/selfadvertisement.html.twig' %}
                    {% endif %}

                {% endblock %}
            </div>
            <div class="col-md-{% if columns2|default(false) %}9{% else %}6{% endif %} col-sm-6 church-site-main-content">
                {% block pageHeader %}
                    <div class="page-header">
                        <h2>{{ title|raw }}</h2>
                    </div>
                {% endblock %}

                {% block messages %}
                    {% if messages|default([])|length > 0 %}
                        <div id="messages" style="margin-bottom:6px">
                            {% for message in messages %}
                                {% include 'message.twig' with message %}
                            {% endfor %}
                        </div>
                    {% endif %}
                {% endblock %}
                {% block content %}{% endblock %}
                <br/>
            </div>
            {% if columns2|default(false) %}{% else %}
                <div class="col-md-3 col-sm-3 church-site-right-sidebar">
                    {% block rightsidebar %}{% endblock %}
                </div>
            {% endif %}
        </div>
    </div>
    <div class="footer">
        <div class="container">
            <div class="row">
                <div class="col col-md-3 col-sm-3">
                    <img src="{{ asset('static/images/logo-feher.png') }}" loading="lazy"/>
                </div>
                <div class="col">
                    <p>Fejlesztés: <a href="https://github.com/borazslo/miserend.hu">GitHub</a>
                        {%- if githash -%}
                            {{ ' - ' -}}
                            <small>verzió: <a href="https://github.com/borazslo/miserend.hu/commit/{{ githash }}">
                                    {{- githash[0:7] -}}
                                </a></small>
                        {% endif -%}
                        | <a href="/impresszum">Impresszum</a> | <a href="/stat">Statisztika és adatok</a>
                    </p>

                    <p class="mobilapps">
                        <a href="https://geo.itunes.apple.com/us/app/miserend/id967827488?mt=8"
                           onclick="ga('send', 'event', 'Advertisment', 'itunes.apple.com', 'liladoboz-kep')">
                            <img src="{{ asset('static/images/app_store_images/apple.svg') }}" alt="Töltsd le az App Store-ból">
                        </a>
                        <a href="https://play.google.com/store/apps/details?id=com.frama.miserend.hu"
                           onclick="ga('send', 'event', 'Advertisment', 'play.google.com', 'liladoboz-kep')">
                            <img src="{{ asset('static/images/app_store_images/google.svg') }}" alt="Töltsd le a Google Play-ről"></a>
                    </p>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

<script language="JavaScript" type="text/javascript">
    var trackOutboundLink = function (url) {
        ga('send', 'event', 'outbound', 'click', url, {
            'hitCallback':
                function () {
                    document.location = url;
                }
        });
    }
</script>
</body>
</html>
