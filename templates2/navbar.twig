{% set externalIcon %}<span class="glyphicon glyphicon-new-window"></span>{% endset %}

<nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar">
                <span class="sr-only">Menü</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="http://localhost/miserend.hu/">Miserend.hu</a>
        </div>

        <div class="collapse navbar-collapse" id="navbar">
            <ul class="nav navbar-nav">   
                {% if adminmenu %}
                    {% for items in adminmenu %}
                        <li class="dropdown">
                            <a href="{{ items.url }}" class="dropdown-toggle" data-toggle="dropdown">{{ items.title }}<span class="caret"></span></a>
                            {% if items.items %}
                                <ul class="dropdown-menu">
                                    {% for item in items.items %}
                                        <li><a href="{{ item.url }}">{{ item.title }}</a></li>
                                    {% endfor %}
                                </ul>
                            {% endif %}
                        </li>
                    {% endfor %}
                {% endif %}
                <li class="dropdown">
                    <a href="?template=templates">Régi design</a>
                </li>
            </ul>
            {% if user.loggedin %}
                <form class="navbar-form navbar-right" role="search"  method="post">
                    <a class="button btn btn-default" href="?m_id=28">{{ user.username }}</a>
                    <a class="button btn btn-default" href="?kilep=true" id="quit">Kilépés</a>
                </form>
            {% else %}
                <form class="navbar-form navbar-right" role="search"  method="post">
                    <div class="form-group">
                        <input type="text" class="form-control" name="login" placeholder="Név" size="10">
                    </div>
                    <div class="form-group">
                        <input type="password" class="form-control" name="passw" placeholder="Jelszó" size="10">
                    </div>
                    <button type="submit" class="btn btn-default">Belépés</button>
                    <a class="button btn btn-default" href="?m_id=28">Regisztrálás</a>
                </form>
            {% endif %}
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>
